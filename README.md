# 🏋️‍♂️ Training-Notification Service

Микросервис системы уведомлений для фитнес-платформы. Проект построен на событийно-ориентированной архитектуре (Event-Driven) и отвечает за информирование пользователей о тренировках и рассылку аналитических отчетов.

## 🚀 Основные возможности

* **обработка событий Kafka**: Сервис слушает топик `training-events` и мгновенно обрабатывает данные о новых или завершенных тренировках.
* **Умное кэширование (Redis)**: Использование `@Cacheable` в `UserLookupService` позволяет минимизировать запросы к основной базе данных при поиске email пользователей.
* **Еженедельные отчеты по расписанию**: Реализован `WeeklyReportSchedular`, который каждое воскресенье в 20:00 собирает статистику (калории, время, достижения) и делает массовую рассылку.
* **Асинхронная отправка Email**: Благодаря аннотации `@Async` и `JavaMailSender`, процесс отправки писем не блокирует основной поток обработки событий.
* **Логирование уведомлений**: Каждое отправленное сообщение фиксируется в базе данных через `NotificationLogRepository` для последующего аудита.

## 🛠 Технологический стек

* **Java 17** (использование современных `record` для DTO).
* **Spring Boot 3** (Web, Data JPA, Mail, Cache).
* **Apache Kafka**: Брокер сообщений для получения событий.
* **Redis**: Кэширование данных пользователей.
* **PostgreSQL**: Персистентное хранение пользователей и логов.
* **Thymeleaf**: HTML-шаблоны для профессионально оформленных писем.
* **Lombok**: Сокращение шаблонного кода.

## 📁 Ключевые компоненты

* `NotificationService`: Сердце системы, обрабатывающее входящие сообщения из Kafka.
* `EmailNotificationService`: Реализация интерфейса `NotificationSender` для работы с SMTP.
* `WeeklyReportSchedular`: Компонент для автоматизации бизнес-процессов по расписанию.
* `UserLookupService`: Сервис-прослойка с поддержкой кэширования.

## ⚙️ Запуск проекта

1. Убедитесь, что у вас запущены **PostgreSQL**, **Kafka** и **Redis**.
2. Настройте параметры подключения в `src/main/resources/application.properties`:
   ```properties
   spring.mail.username=your_email@gmail.com
   spring.mail.password=your_app_password
   spring.kafka.bootstrap-servers=localhost:9092
   spring.data.redis.host=localhost